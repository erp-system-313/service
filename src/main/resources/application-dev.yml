spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/erp
    username: erp
    password: erp
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true

logging:
  level:
    com.erp: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
